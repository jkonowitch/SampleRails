<h1>All Actors</h1>
<% @actors.each do |actor| %>
  <div class="actor">
    <p><%= actor.name %></p>
    <img src="<%= actor.photo_url %>" />
    <%= link_to 'Delete', actor_path(actor), :method => :delete %>
  </div>
<% end %>